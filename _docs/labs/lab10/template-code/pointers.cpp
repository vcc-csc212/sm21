#include "pointers.h"
#include <cstring>

void Pointers::bookends(const std::string* arr, int len, std::string (&ends)[2]) {
    /*
        This function should take the first and last elements of arr and store them in ends
        len is the size of arr, and you can assume ends is of size 2.
    */
}
        
void Pointers::switcharoo(std::string* arr, int idx, std::string str) {
    /*
        This function should replace the value at index idx in arr with the value in str
    */
}